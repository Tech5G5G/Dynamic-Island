<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Dynamic_Island.Settings.Pages.Animations"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dynamic_Island.Settings.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="8" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Text="Opening&#xA;animations" VerticalAlignment="Center" />
        <ComboBox x:Name="openMode" Header="Mode" PlaceholderText="Mode" Grid.Column="1">
            <x:String>Ease out</x:String>
            <x:String>Ease in</x:String>
            <x:String>Ease in out</x:String>
        </ComboBox>
        <ComboBox x:Name="openType" Header="Type" PlaceholderText="Type" ItemsSource="{x:Bind easingTypes}" Grid.Column="2" />
        <NumberBox x:Name="openDuration" Header="Duration (ms)" PlaceholderText="Duration" SpinButtonPlacementMode="Compact" SmallChange="10" LargeChange="10" Grid.Column="3" ValueChanged="Number_ValueChanging" />
        <TextBlock Text="Closing&#xA;animations" VerticalAlignment="Center" Grid.Row="1" />
        <ComboBox x:Name="closeMode" Header="Mode" PlaceholderText="Mode" Grid.Row="1" Grid.Column="1">
            <x:String>Ease out</x:String>
            <x:String>Ease in</x:String>
            <x:String>Ease in out</x:String>
        </ComboBox>
        <ComboBox x:Name="closeType" Header="Type" PlaceholderText="Type" ItemsSource="{x:Bind easingTypes}" Grid.Row="1" Grid.Column="2" />
        <NumberBox x:Name="closeDuration" Header="Duration (ms)" PlaceholderText="Duration" SpinButtonPlacementMode="Compact" SmallChange="10" LargeChange="10" Grid.Row="1" Grid.Column="3" ValueChanged="Number_ValueChanging" />
    </Grid>
</Page>
